services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: gem_verification
      POSTGRES_USER: gem_user
      POSTGRES_PASSWORD: gem_pass
    ports: ["5432:5432"]

  redis:
    image: redis:7
    ports: ["6379:6379"]